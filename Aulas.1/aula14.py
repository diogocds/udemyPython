a = 'A'
b = 'B'
c = 1.1


string = 'a={} b={} c ={:.1f}'    # o colchet puxa os valores existentes especificados na função format.
formato = string .format(a, b, c) # parametro nomeado é quando se dar nome as funções criadas Ex: strings etc...
                                  # format é um método da viriável string

print(formato)